# GitHub Upload Instructions for Nalanda Chatbot 2.0

## ✅ Git Repository Initialized!

Your local git repository is ready with all files committed.

## 📤 Push to GitHub - Follow These Steps:

### Step 1: Create GitHub Repository

1. Go to https://github.com/new
2. **Repository name:** `nalanda_chatbot_2.0`
3. **Description:** "Secure AI-powered library assistant for IIT Ropar - Book search, smart suggestions, 100/100 security"
4. **Visibility:** Public (or Private if you prefer)
5. **DO NOT** check "Initialize with README" (we already have one)
6. Click **"Create repository"**

### Step 2: Connect Local Repository to GitHub

Open PowerShell in the `lib_chat` folder and run:

```bash
cd "C:\xamppp\htdocs\library\lib_chat"
git remote add origin https://github.com/YOUR_USERNAME/nalanda_chatbot_2.0.git
git branch -M main
git push -u origin main
```

**Replace `YOUR_USERNAME`** with your actual GitHub username.

### Step 3: Enter GitHub Credentials

When prompted:
- **Username:** Your GitHub username
- **Password:** Use a **Personal Access Token** (not your password)

**To create a Personal Access Token:**
1. Go to https://github.com/settings/tokens
2. Click "Generate new token" → "Generate new token (classic)"
3. Give it a name: "Nalanda Chatbot Upload"
4. Select scopes: Check **"repo"** (full control)
5. Click "Generate token"
6. Copy the token (you won't see it again!)
7. Use this token as your password when pushing

### Step 4: Verify Upload

After pushing, visit:
```
https://github.com/YOUR_USERNAME/nalanda_chatbot_2.0
```

You should see all files uploaded!

---

## 🎯 Quick Command Summary

```bash
# 1. Navigate to directory
cd "C:\xamppp\htdocs\library\lib_chat"

# 2. Add GitHub remote (replace YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/nalanda_chatbot_2.0.git

# 3. Push to GitHub
git branch -M main
git push -u origin main
```

---

## 📦 What's Being Uploaded

**16 files, 7,245 lines of code:**
- ✅ README.md (comprehensive documentation)
- ✅ .gitignore (excludes cache and logs)
- ✅ All chatbot code (JS, PHP, CSS)
- ✅ Book database (acc register.xlsx)
- ✅ FAQ database (general_queries.json)
- ✅ Security utilities (rate limiter, CSRF)
- ✅ HTTPS setup guide

**Excluded (by .gitignore):**
- ❌ Cache files (auto-generated)
- ❌ Query logs (user data)
- ❌ Temporary files

---

## 🚀 After Upload

### Add Topics/Tags on GitHub:
- `chatbot`
- `library`
- `php`
- `javascript`
- `ai`
- `opac`
- `book-search`
- `security`

### Add GitHub Description:
"🤖 Nalanda Chatbot 2.0 - Secure AI-powered library assistant for IIT Ropar featuring book search (5000+ books), smart suggestions, and general library inquiries. 100/100 security score with HTTPS, CORS, rate limiting, and XSS protection."

---

## 🔄 Future Updates

When you make changes:

```bash
cd "C:\xamppp\htdocs\library\lib_chat"
git add .
git commit -m "Description of changes"
git push
```

---

## 🎉 Repository Ready!

Your Nalanda Chatbot 2.0 is:
- ✅ Git initialized
- ✅ All files committed
- ✅ README.md created
- ✅ .gitignore configured
- ✅ Ready to push to GitHub

**Next:** Follow Step 1 above to create the GitHub repository!
